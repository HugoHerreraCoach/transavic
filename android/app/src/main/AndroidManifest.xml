<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Mantiene la CPU despierta para que el GPS siga reportando con la pantalla
         apagada (lo usa el foreground service de location). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- NOTA: los permisos de ubicación + foreground service + notificación los APORTA
         el plugin @capacitor-community/background-geolocation desde su propio manifest
         (se fusionan automáticamente): ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION,
         FOREGROUND_SERVICE, FOREGROUND_SERVICE_LOCATION, POST_NOTIFICATIONS y el
         <service> BackgroundGeolocationService (foregroundServiceType="location").
         No hace falta redeclararlos aquí.

         A PROPÓSITO no usamos ACCESS_BACKGROUND_LOCATION: el plugin rastrea en segundo
         plano mediante el foreground service con permiso de ubicación "mientras se usa",
         lo que EVITA la revisión especial de "ubicación en segundo plano" de Google Play
         (declaración + video). Es la forma recomendada y más fácil de aprobar. -->
</manifest>
